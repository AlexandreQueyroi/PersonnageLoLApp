<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PersonnageLoLApp"
             x:Class="PersonnageLoLApp.MainPage">

    <VerticalStackLayout Padding="20">
        <Label Text="Personnages League of Legends" FontSize="22" FontAttributes="Bold" />
    
        <Button Text="Charger les personnages" Clicked="OnLoadClicked" />
        

        <CollectionView x:Name="PersonnagesList" Margin="0,20,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Gray" Padding="10" Margin="5">
                        <VerticalStackLayout>
                            <Label Text="{Binding Nom}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding Role}" />
                            <Label Text="{Binding PV, StringFormat='PV : {0}'}" />
                            <Label Text="{Binding Attaque, StringFormat='Attaque : {0}'}" />
                            <Button Text="Supprimer"
                                    BackgroundColor="Red"
                                    TextColor="White"
                                    CornerRadius="10"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=BindingContext.DeleteCommand}"
                                    CommandParameter="{Binding Id}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
